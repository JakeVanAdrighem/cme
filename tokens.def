enum TOK_TYPE {
// Preprocessor Tokens
PTOK_IF,
// Keywords
TOK_FOR,
TOK_WHILE,
TOK_DOWHILE,
TOK_IF,
TOK_THEN,
TOK_ELSE,
TOK_SWITCH,
TOK_GOTO,
TOK_RETURN,
TOK_INT,
TOK_CHAR,
TOK_BOOL,
TOK_FLOAT,
// String Literal
TOK_STRING_LITERAL,
// Integer constant
TOK_INTEGER_CONSTANT,
// Character contant
TOK_CHARACTER_CONSTANT,
//Punctuator Symbols
TOK_LSQUARE,
TOK_RSQUARE,
TOK_LPAREN,
TOK_RPAREN,
TOK_LBRACE,
TOK_RBRACE,
TOK_DOT,
TOK_INCREMENT,
TOK_DECREMENT,
TOK_DEREFERENCE,
TOK_AMPERSAND,
TOK_SEMICOLON,
TOK_BANG,
TOK_PIPE,
TOK_PLUS,
TOK_DASH,
TOK_STAR,
TOK_FORWARDSLASH,
TOK_BACKSLASH,
};
